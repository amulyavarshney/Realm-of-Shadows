# Realm of Shadows — environment template
# Copy to .env and restart Expo after changes.

# Online multiplayer API (required for Host / Join)
# Local dev: http://YOUR_LAN_IP:8000  (not localhost on physical devices)
# Production: https://your-api.example.com
EXPO_PUBLIC_BACKEND_URL=http://localhost:8000
